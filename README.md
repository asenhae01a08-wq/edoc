# edoc

iniciando projeto
construir ambiente virtual com o comando: py -m venv .venv
depois ativar o ambiente virtual com o comando: .venv/Scripts/activate
depois instalar as dependencias: pip install flask python-docx mysql-connector-python
depois executar o comando de inicialização: 
flask --app app run --debug
